package ck;

import ergoline::bool;

@system(alias="ck::future") class future<T> {
    @system def set(t: T);
    @system @sync def get(): T;
    @system(alias="is_ready") def isReady(): bool;
    @system def release();
    @system def apply(): future<T>;
}
