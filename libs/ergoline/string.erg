package ergoline;

@system class string {
    @system def toString(self: string): string;
    @system def plus(self: string, other: string): string;
    @system def tryParse<A>(self: string): option<A>;
}
