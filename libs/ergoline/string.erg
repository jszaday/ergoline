package ergoline;

@system(alias = "std::string") class string {
    @system def toString(): string;
    @system def plus(other: string): string;

    @system(alias = "std::stoi", static = true)
    def toInt(): int;

    @system def tryParse<A>(): option<A>;
}
